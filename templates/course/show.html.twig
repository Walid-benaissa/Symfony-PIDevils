{% extends 'base-front.html.twig' %}

{% block body %}
  <h1>Course</h1>
  <div class="table-responsive">
    <table class="table table-striped">
      <tbody>
        <tr>
          <th>IdCourse</th>
          <td>{{ course.idCourse }}</td>
        </tr>
        <tr>
          <th>Point de départ</th>
          <td>{{ course.pointDepart }}</td>
        </tr>
        <tr>
          <th>Point de destination</th>
          <td>{{ course.pointDestination }}</td>
        </tr>
        <tr>
          <th>Distance</th>
          <td>{{ course.distance }}</td>
        </tr>
        <tr>
          <th>Prix</th>
          <td>{{ course.prix }}</td>
        </tr>
        <tr>
          <th>Statut de la course</th>
          <td>{{ course.statutCourse }}</td>
        </tr>
      </tbody>
    </table>
  </div>
  <a href="{{ path('app_course_showfr') }}" class="btn btn-primary">Retour</a>
  <a href="{{ path('app_course_edit', {'idCourse': course.idCourse}) }}" class="btn btn-secondary">Modifier</a>
  
  
    
  

  {{ include('course/_delete_form.html.twig') }}
{% endblock %}
