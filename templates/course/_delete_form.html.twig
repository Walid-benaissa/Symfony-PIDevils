<form method="post" action="{{ path('app_course_delete', {'idCourse': course.idCourse}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ course.idCourse) }}">
    <button class="btn">Delete</button>
</form>
