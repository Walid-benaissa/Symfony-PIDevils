<form method="post" action="{{ path('app_offre_course_delete', {'idOffre': offre_course.idOffre}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offre_course.idOffre) }}">
    <button class="btn">Delete</button>
</form>
