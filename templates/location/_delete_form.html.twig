<form method="post" action="{{ path('app_location_delete', {'idContrat': location.idContrat}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ location.idContrat) }}">
    <button class="btn">Delete</button>
</form>
