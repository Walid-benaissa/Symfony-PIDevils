{% extends 'base-back.html.twig'%}
{% block body %}
    <h1>Commentaire</h1>

    <link href="{{asset("css/cardreclamation.css")}}" rel="stylesheet">
	<div class="container bod">
    <a class="col-1 btn btn-primary" href="{{ path('app_commentaire_index') }}"> Retour </a>

    <h1 class="title">Reclamations</h1>
    <div class="row">
    <table class="table">
		<tbody>
			<tr>
				<th>Id</th>
					<td>{{ commentaire.id }}</td>
			</tr>
			<tr>
				<th>Effectué par</th>
					<td>{{ commentaire.id1.nom ~ " " ~ commentaire.id1.prenom }}</td>
			</tr>
				<tr>
				<th>Message</th>
					<td>{{ commentaire.message }}</td>
				</tr>
		</tbody>
	</table>

  

    {{ include('commentaire/_delete_form.html.twig') }}
{% endblock %}
