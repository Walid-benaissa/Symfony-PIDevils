{% extends 'base-back.html.twig'%}

{% block body %}
	<h1>Utilisateur</h1>

	<table class="table">
		<tbody>
			
			<tr>
				<th>Nom</th>
				<td>{{ utilisateur.nom }}</td>
			</tr>
			<tr>
				<th>Prénom</th>
				<td>{{ utilisateur.prenom }}</td>
			</tr>
			<tr>
				<th>Mail</th>
				<td>{{ utilisateur.mail }}</td>
			</tr>

			<tr>
				<th>Numéro Téléphone</th>
				<td>{{ utilisateur.numTel }}</td>
			</tr>
			<tr>
				<th>Rôle</th>
				<td>{{ utilisateur.role }}</td>
			</tr>
			<tr>
				<th>Evaluation</th>
				<td>{{ utilisateur.evaluation }}</td>
			</tr>
			<tr>
				<th>Bloque</th>
				<td>{{ utilisateur.bloque ? 'Oui' : 'Non' }}</td>
			</tr>
		</tbody>
	</table>

{% if utilisateur.role == "Conducteur" %}
{# <div class="image-container">
        <img src="{{ asset('photo/'~conducteur.b3) }}" alt="NOM" class="circular-image">
</div>
<div class="image-container">
        <img src="{{ asset('photo/'~conducteur.Permis) }}" alt="NOM" class="circular-image">
</div> #}
{% endif %}

	<a href="{{ path('app_utilisateur_index') }}">retour</a>

	

	{{ include('utilisateur/_delete_form.html.twig') }}
{% endblock %}
