<!DOCTYPE html>

<title>Promotion</title>

{% block body %}
    <h1>Promotion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdPromotion</th>
                <td>{{ promotion.idPromotion }}</td>
            </tr>
            <tr>
                <th>Taux</th>
                <td>{{ promotion.taux }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_promotion_index') }}">back to list</a>

    <a href="{{ path('app_promotion_edit', {'idPromotion': promotion.idPromotion}) }}">edit</a>

    {{ include('promotion/_delete_form.html.twig') }}
{% endblock %}
